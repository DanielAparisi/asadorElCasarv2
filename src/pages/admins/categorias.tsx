/**
 * Categorías de la carta: nombres y orden. Fase 4 de docs/panel.md.
 *
 * Pendiente: lista editable con el campo `orden`. Arrastrar y soltar solo si
 * lo piden; un número editable resuelve el 90% del problema.
 */
function Categorias() {
  return (
    <div>
      <h1 className="text-xl font-semibold mb-6">Categorías</h1>

      <p className="text-gray-500">
        Todavía no hay tabla de categorías: falta crearla en la base de datos.
      </p>
    </div>
  )
}

export default Categorias
